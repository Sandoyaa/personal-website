// The site lives under /personal-website, so the bare root and any stray URL land here: show the home
// page rather than a dead end — also what GitHub Pages serves as 404.html.
export { default } from '@/app/page';
